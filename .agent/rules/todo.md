---
trigger: always_on
---

1. **Type Checking**: Whenever you modify `.ts` or `.tsx` files, you MUST run `npm run tsc` (or `npx tsc --noEmit`) to verify that your changes didn't introduce type errors.
2. **TODO Standard**: Whenever you leave a comment for future reference, ALWAYS mark it with `// TODO:`.
